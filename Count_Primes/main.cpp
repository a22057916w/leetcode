#include <iostream>
#include <vector>

using namespace std;


class Solution {
private:
    vector<int> prime;
public:
    static int countPrimes(int n) {
        vector<int> prime(10, 0);
    }

    static void primelist() {
        vector<bool> vis(false, size);
        for(suto n: vis)
            cout << n << endl;

    }
};

int main() {
  vector<int> v(10, 0);
  //cout << Solution::countPrimes(10) << endl;
  return 0;
}
